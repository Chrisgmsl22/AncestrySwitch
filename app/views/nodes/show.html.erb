<p style="color: green"><%= notice %></p>
 <div class="btn-group" role="group" aria-label="Basic example">
     <%= link_to "Edit this node", edit_node_path(@node), class: "btn btn-success btn-sm"   %>

     <%= link_to "Back to nodes", nodes_path, class: "btn btn-secondary btn-sm" %>

     <%= button_to "Destroy this node", @node, method: :delete, class: "btn btn-danger btn-sm" %>
  </div>
  
  <table class="table table-striped table-hover">
      <%= render "thead" %>
      <%= render  @node %>
      </tr>
    </tbody>
  
  </table>
  